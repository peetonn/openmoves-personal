openmoves beta

To be used atop OpenPTrack. Receives data from OpenPTrack's UDP stream; ensure that your computer is connected to the same network as the OPT system is before use 
To install: 
    navigate to folder
    sudo pip install -e .
remap.ucla.edu

